<%#
# To change this template, choose Tools | Templates
# and open the template in the editor.
%>




<div class="dialogbox">
  <div class="body">

  
<%#  puts "lenth is #{@comment.commenter}"  %>
  
    <span class="tip tip-left"></span>
    <div class="message">
      <span>
        <b>  <%= comment.commenter %>: </b>
       <%= comment.body %><br/>
      </span>
     
   <%= link_to 'Reply', new_comment_path(:parent_id => comment,:article_id => comment.article_id), remote: true, :class => 'replay' %>
  <% if current_user.id == comment.commenter_id %>
 <div style="display:inline-block;">
  <%= button_to 'Destroy', comment_path(comment),remote: true,
              method: :delete,
data: { confirm: 'Are you sure?' } %> </div>
       
  <% end %>
        
  </div>
  </div>
</div>




        <%#*<b>  <%= comment.commenter %>
  <%#= comment.body %>
   <%#= link_to 'Reply', new_comment_path(:parent_id => comment,:article_id => comment.article_id), :class =>'replay' %>
  <%# if current_user.id == comment.commenter_id %>
  <%#= button_to 'Destroy', comment_path(comment),
              method: :delete,
data: { confirm: 'Are you sure?' } %>
  <%# end %>
       





















     



